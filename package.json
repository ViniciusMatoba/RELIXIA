{
  "name": "geek-pixel-idle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "gh-pages": "^6.3.0",
    "phaser": "^3.88.2"
  }
}
